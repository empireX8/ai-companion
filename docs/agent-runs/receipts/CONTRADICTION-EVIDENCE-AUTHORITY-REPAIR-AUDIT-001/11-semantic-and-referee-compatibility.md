# 11 — Semantic and referee compatibility

## Semantic classifications
Compatible (`compatible_states`) and ambiguous (`insufficient_or_misaligned_context`) remain non-Class-A; referee not run; persistenceDecision null.

Clear contradiction still requires valid bound evidence for both ordered sides.

Evidence selections remain required in transport for all classifications (same as pre-CEQR-016 contract). Binding applies whenever parse succeeds.

## Objectivity Referee
Receives `validatedSemanticResult` only after binding + validation succeed for Class A.
Does not depend on provider-authored `sourceId`/`exactQuote`.
