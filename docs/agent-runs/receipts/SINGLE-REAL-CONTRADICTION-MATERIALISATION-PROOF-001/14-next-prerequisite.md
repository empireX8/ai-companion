# 14 — Next prerequisite

**Campaign:** SINGLE-REAL-CONTRADICTION-MATERIALISATION-PROOF-001 (closeout)
**Wave 2.1 result:** FAIL — NO TRUSTWORTHY CONTRADICTION CANDIDATE FOUND
**This document:** prerequisite definition only — **no design or implementation**

---

## NEXT PREREQUISITE

**CONTRADICTION EXTRACTION QUALITY REPAIR**

Wave 2.1 cannot be re-attempted on Kay's account with the current **25** pending ContradictionNode rows without risking acceptance of overinterpreted or compatible-state pairs. The accept → open → UEL → ModelUpdate → Map → Inspector **pathways are landed and unit-proven**; the **import-derived candidate quality** is the blocker.

---

## Minimum repair scope (later campaign)

The extraction-quality repair wave must address at least:

| # | Requirement |
|---|-------------|
| 1 | **Semantic contradiction assessment** rather than generic substring marker matching (`but i`, `however i`, `even though`, goal-failure tokens alone) |
| 2 | **Cross-session pairing policy** — session-scoped or explicit provenance-scoped pairing; reject or flag pairs where Side A and Side B originate from different conversations unless both sides have explicit machine-readable lineage preserved on the CN |
| 3 | **Side A and Side B source-level lineage** — persist or surface both message/session (or evidence-span) ids on the ContradictionNode or equivalent truthful Inspector contract |
| 4 | **Qualification/context preservation** — do not pair statements whose qualifying clauses (`although`, partial compliance, self-uncertainty) resolve or weaken the apparent tension |
| 5 | **Compatible-state rejection** — reject pairs classifiable as simultaneous compatible modes (e.g. cognitive disengagement + physiological sensitivity) |
| 6 | **Goal-versus-obstacle distinction** — separate stated goals from obstacles/doubts that do not constitute behavioral violation |
| 7 | **Duplicate candidate prevention** — tighten fanout caps, shared Side B message collisions, and repeated Side A constraint refs across sessions |
| 8 | **Confidence and uncertainty calibration** — align `confidence` with semantic assessment; avoid presenting low-quality heuristic pairs as review-ready tensions |
| 9 | **Read-only re-evaluation of existing 25 candidates** — classify/repair without Accept/Reject in the repair campaign's first phase |
| 10 | **New candidate only through controlled natural-entry proof** — after repair, any new trustworthy candidate must enter via corrected import/derivation, not manual DB patch or silent detector tweak during a human proof campaign |

---

## Explicitly out of scope for the repair campaign (when defined)

- Wave 2.1 human Accept on Kay account (waits until ≥1 proof-eligible candidate exists)
- ReferenceItem acceptance
- Bulk reject/archive of the 25 pending rows without Kay-directed policy
- ProfileArtifact / goals / uploader / unrelated surfaces
- Production-readiness claim

---

## Suggested sequence after repair (documentation only)

```
CONTRADICTION EXTRACTION QUALITY REPAIR
  → read-only re-evaluate 25 (+ any new import output)
  → at least one proof-eligible candidate established
  → re-run Wave 2.1 (new campaign receipt set)
  → single deliberate human Accept
  → after-state verification
```

---

## What Wave 2.1 proved despite FAIL

| Proven | Not proven |
|--------|------------|
| Read-only audit methodology for CN semantic quality | One genuine Kay CN accept |
| Detector defects documented with exemplar | Open genuine CN on Kay account |
| Accept path code trace complete (`02`) | Map/Inspector on live accepted CN |
| Account gate stable across campaign | Human materialisation proof |
| Wave 1.1 read projection remains valid | Production readiness |

---

## Production readiness

**NO**
