# Surface Language Matrix

| Severity | Surface | File | Current text / snippet | Route / component | Failed rule | Why it fails | Correction direction | Fix before visual work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BLOCKER | Decisions / Actions | `components/orvek-v0/pages/decisions.tsx` | `Enter a decision, see what to choose, record what happened, and learn what it reveals.` / `What Orvek would choose` | `/actions` via `app/(root)/(routes)/actions/page.tsx` -> `components/orvek-workbench/OrvekDecisionsPage.tsx` -> `components/orvek-v0/pages/decisions.tsx` | 2B, 2C | The surface speaks as if Orvek can decide for the user and tells the user what to choose. That is verdict language, not a model proposal. | Replace with proposal-oriented copy rooted in receipts/evidence, or make the choice projection honestly unavailable until that contract exists. | Yes |
| HIGH | Journal Chat | `app/(root)/(routes)/journal-chat/page.tsx` | `Journal Chat`, `Guided reflection`, `Reflective mode`, `Recent entry`, `Memory`, `Pattern surfaced`, `Use the Memory panel in \`/chat\`` | `/journal-chat` | 2A, 2C | The visible framing is journaling / reflection / memory-panel language, which reintroduces the wrong product model and also references a blocked legacy route. | Reframe the surface as raw capture or inspectable evidence, or make it honestly unavailable/local-only if no valid v0 contract exists yet. | Yes |
| MEDIUM | What Changed report | `components/orvek-v0/pages/what-changed.tsx`, `lib/what-changed-surface.ts`, `components/orvek-workbench/OrvekWhatChangedPage.tsx`, `app/(root)/(routes)/what-changed/page.tsx` | `What changed`, `Why ... thinks this`, `Evidence behind it`, `Where to re-enter` | `/what-changed` | 2D | The report surface is under-structured relative to the locked report grammar. It exposes only a subset of the required sections. | Expand the surface to the full report grammar, or label it honestly incomplete/unavailable until the missing sections exist. | Yes |
| MEDIUM | Today / Re-entry | `components/orvek-v0/pages/today.tsx`, `lib/today-reentry.ts`, `components/orvek-workbench/OrvekTodayPage.tsx` | `Most consequential now`, `Needs attention now`, `Fieldwork / Watch For`, `Open loops / active questions`, `Resurfaced receipts`, `Report ready`, `What changed`, `Linked receipts`, `Last evidence` | `/` via `components/orvek-workbench/OrvekTodayPage.tsx` -> `components/orvek-v0/pages/today.tsx` | 2D | The surface uses re-entry buckets and summary cards, not the locked Today grammar (`State`, `Delta`, `Why it matters`, `Evidence pointer`, `Next observation / test`, `Re-entry trigger`, `Capture path`). | Rewrite the Today contract or make the surface honestly unavailable/local-only until the full grammar exists. | Yes |

